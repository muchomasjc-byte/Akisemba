{
  "name": "akisemba",
  "version": "1.0.0",
  "description": "RitmoBoleto — venta de entradas de baile",
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
